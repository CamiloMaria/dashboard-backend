/**
 * Standard log types for user activity logging
 */
export enum LogType {
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
  LOGIN = 'LOGIN',
  LOGOUT = 'LOGOUT',
  PERMISSION = 'PERMISSION',
  VIEW = 'VIEW',
}
